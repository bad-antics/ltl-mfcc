// examples/demo.ltl — runnable demo for ltl_mfcc

import std::io
use ltl_mfcc as lib

fn main() {
    io::println("ltl_mfcc demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}